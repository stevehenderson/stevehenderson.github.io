source "https://rubygems.org"

gem "github-pages"

# Required for `jekyll serve` on Ruby 3+ (webrick was removed from stdlib).
# GitHub Pages ignores this gem when building, so it only affects local preview.
gem "webrick", "~> 1.8"
